<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.suiyi.main">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <dist:module dist:instant="true" />

    <application
        android:name="com.suiyi.main.SHApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- AutoSize 框架使用 -->
        <meta-data android:name="design_width_in_dp" android:value="367" />

        <activity android:name="com.suiyi.main.activity.CartActivity" >

        </activity>

        <activity android:name="com.suiyi.main.EntranceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.CoroutineScopeActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity android:name=".activity.SecondFloorActivity" />

        <activity android:name=".activity.ScrollerActivity" />

        <activity android:name=".activity.RVIndicatorActivity" />

        <activity android:name=".activity.RecyclerReuseBugActivity" />

        <activity android:name=".activity.FlutterTestActivity"
            android:theme="@style/Theme.AppCompat.DayNight"/>

        <activity android:name=".activity.LottieActivity" />

        <activity android:name=".activity.NestedViewPagerActivity" android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity android:name=".activity.SVGAPlayerActivity" />

        <activity android:name=".activity.JetPackActivity" android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity android:name=".activity.ContextTestActivity" android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity android:name=".activity.RecyclerDiffActivity" />

        <activity android:name=".activity.WebPActivity" />

        <activity android:name=".activity.StaggerBugActivity" />

        <activity android:name=".activity.DownloadSkinBagActivity" />

        <meta-data
            android:name="environment"
            android:value="${environment}" />

    </application>

</manifest>